workers = 4

tcp_port = 8888

ssl = True
ssl_port = 8443
ssl_key  = 'server.key'
ssl_crt  = 'server.crt'

default = {'mode': 'socks5', 'host': '127.0.0.1', 'port': 1234}

country_rules = {
	'cn': {'mode': 'pass'},
}

hostname_fules = {
	'wp.com': default,
        'twitter.com': default,
        'facebook.com': default,
        'www.facebook.com': default,
        'www.twitter.com': default,
        'plus.google.com': default,
}
